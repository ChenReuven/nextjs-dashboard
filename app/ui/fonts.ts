import { Inter, IBM_Plex_Mono } from 'next/font/google';

export  const inter = Inter({subsets: ['latin'], weight: "400"});
export  const ibmFlexMono = IBM_Plex_Mono({subsets: ['latin'], weight: "700"});
